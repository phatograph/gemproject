<div class="page-header">
  <h2>{{user.id ? 'Edit ' + user.fullName : 'Add new user'}}</h2>
</div>

<div class="row">
  <div class="col-xs-6 col-xs-offset-2">
    <form class="form-horizontal">
      <%= render :partial => 'templates/forms/text', :object => 'firstName', :locals => {} %>
      <%= render :partial => 'templates/forms/text', :object => 'lastName', :locals => {} %>
      <%= render :partial => 'templates/forms/text', :object => 'email', :locals => {:type => 'email'} %>
      <%= render :partial => 'templates/forms/text', :object => 'employeeNumber', :locals => {} %>
      <%= render :partial => 'templates/forms/text', :object => 'position', :locals => {} %>
      <%= render :partial => 'templates/forms/text', :object => 'department', :locals => {} %>
      <div class="panel panel-default">
        <div class="panel-body">
          <%= render :partial => 'templates/forms/text', :object => 'password', :locals => {:type => 'password'} %>
          <div ng-hide="user.id">
            <%= render :partial => 'templates/forms/text', :object => 'confirmPassword', :locals => {:type => 'password'} %>
          </div>
        </div>
      </div>
      <div class="form-group">
        <div class="col-xs-offset-3 col-xs-9">
          <button ng-click="saveUser()" type="submit" ng-disabled="!(user.id || (user.password && user.password == user.confirmPassword))" class="btn btn-success">Submit</button>
          <%= link_to 'Back', '/', :class => 'btn btn-default' %>
        </div>
      </div>
    </form>
  </div>
</div>
